import Ember from 'ember';

export default Ember.Service.extend({
  /*
  currentUser: null,
  login(asesor){
    this.set("currentUser", asesor);
  }

  logout(){
    this.set("currentUser", null);
  }
  */
});
